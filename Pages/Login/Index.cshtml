@page
@model ASAG_ILIK_nvOM.Pages.Login.IndexModel
@{
    ViewData["Title"] = "Giriş";
}

<div class="text-center">
    <h1 class="display-4">Giriş</h1>

    <form method="post" class="col-4 mx-auto">
        <div class="form-group">
            <label asp-for="UserModel.Username">Kullanıcı Adı:</label>
            <input asp-for="UserModel.Username" class="form-control" required />
        </div>

        <div class="form-group">
            <label asp-for="UserModel.Password">Parola:</label>
            <input asp-for="UserModel.Password" type="password" class="form-control" required />
        </div>
        <br />
        <button type="submit" class="btn btn-primary">Giriş Yap</button>
    </form>
</div>
